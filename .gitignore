# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Node / TypeScript
node_modules/
*.tsbuildinfo

# datastores / misc
*.db
*.sqlite
.DS_Store

# context runtime artifacts
app.log
.prs/

# launch prep (local only, not part of the published repo)
_launch/

# local recording aid — a known-good watchdog to record the demo against
watchdog.reference.*
